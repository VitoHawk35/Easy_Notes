<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 底层：下划线 -->
    <item android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="#00000000"/> <!-- 透明 -->
            <stroke android:width="1dp" android:color="#DDDDDD"/> <!-- 灰色边框 -->
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/> <!-- 遮挡，制造下划线效果 -->
            <padding android:bottom="2dp"/>
        </shape>
    </item>

    <!-- 🟢 [核心修改] 顶层：直接在 item 中引用 vector drawable -->
    <!-- 注意：不要用 <bitmap>，直接用 android:drawable -->
    <!-- android:end="0dp" 和 android:gravity="end" 确保它在最右边 -->
    <item
        android:gravity="center_vertical|end"
        android:end="0dp"
        android:drawable="@drawable/ic_arrow_down_small" />

</layer-list>